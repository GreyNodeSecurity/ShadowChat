"""
ShadowChat: A Peer-to-Peer Encrypted Messaging Application.

Modules:
- `client.py`: Handles the client-side networking and communication.
- `config.py`: Manages configuration settings and loading.
- `discovery.py`: Implements peer discovery and friend request features.
- `gui.py`: Provides the graphical user interface for the application.
- `keygen.py`: Generates RSA key pairs for login and communication.
- `login.py`: Handles user authentication and private key decryption.
- `main.py`: Entry point for launching the ShadowChat application.
- `server.py`: Manages server-side communication and message handling.

To get started, run `main.py`.
"""
